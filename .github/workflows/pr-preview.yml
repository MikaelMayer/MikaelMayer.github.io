name: PR Preview (GitHub Pages)

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: pr-preview-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  deploy_preview:
    name: Deploy PR preview to master subfolder
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name == github.repository
    permissions:
      contents: write
      pull-requests: write
    env:
      # If this is a user/organization Pages repo (<owner>.github.io), Pages is served at the root.
      # Otherwise it's served under /<repo>/.
      PAGES_BASE_URL: ${{ github.event.repository.name == format('{0}.github.io', github.repository_owner) && format('https://{0}.github.io', github.repository_owner) || format('https://{0}.github.io/{1}', github.repository_owner, github.event.repository.name) }}
    steps:
      - name: Checkout PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      # Static site: deploy this app as-is.
      # If you later add a build step, build here and deploy the build output folder.

      - name: Deploy preview to master (Pages source: master)
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: master
          folder: apps/reflex4you
          target-folder: pr-preview/pr-${{ github.event.pull_request.number }}
          enable_jekyll: false
          # Never wipe master; only replace the target-folder contents.
          clean: false
          single-commit: false

      - name: Comment preview URL on PR
        id: preview-comment
        uses: peter-evans/find-comment@v3
        with:
          issue-number: ${{ github.event.pull_request.number }}
          comment-author: github-actions[bot]
          body-includes: "PR Preview deployed"

      - name: Create or update PR comment with preview URL
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.pull_request.number }}
          comment-id: ${{ steps.preview-comment.outputs.comment-id }}
          edit-mode: replace
          body: |
            ✅ **PR Preview deployed**

            - **Preview URL**: ${{ env.PAGES_BASE_URL }}/pr-preview/pr-${{ github.event.pull_request.number }}/
            - **Commit**: ${{ github.event.pull_request.head.sha }}

  no_preview_for_forks:
    name: Inform forks that previews are disabled
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name != github.repository
    permissions:
      pull-requests: write
    steps:
      - name: Find existing "skipped" comment
        id: skipped-comment
        uses: peter-evans/find-comment@v3
        with:
          issue-number: ${{ github.event.pull_request.number }}
          comment-author: github-actions[bot]
          body-includes: "PR Preview not deployed"

      - name: Create or update comment why preview is skipped
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.pull_request.number }}
          comment-id: ${{ steps.skipped-comment.outputs.comment-id }}
          edit-mode: replace
          body: |
            ⚠️ **PR Preview not deployed**

            This PR comes from a fork, so GitHub restricts `GITHUB_TOKEN` permissions and the workflow cannot publish to `gh-pages`.
